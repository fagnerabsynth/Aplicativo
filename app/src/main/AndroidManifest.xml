<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.fagnerabsynth.aplicativo" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Inicial"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Views.Login"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".Views.Principal"
            android:label="@string/title_activity_principal" />
        <activity
            android:name=".Views.Pesquisa"
            android:label="@string/title_activity_pesquisa"></activity>
        <activity
            android:name=".Views.Edicao"
            android:label="@string/title_activity_edicao"></activity>
    </application>

</manifest>
